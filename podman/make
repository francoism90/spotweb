#!/bin/sh

# Build images

podman build \
    -t spotweb-nginx \
    --env=../../.env \
    --file=build/nginx/Dockerfile \
    --no-cache

podman build \
    -t spotweb-app \
    --env=../../.env \
    --file=build/app/Dockerfile \
    --no-cache
